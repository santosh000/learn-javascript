// 1st of all lets see how many windows object

console.log(window);  // u can see in console the list of object that windows have.

// singe element  (grab a single element)

console.log( document.getElementById('my-form')); //grab my-form id from index.html

// Multiple element  (grab multiple elements)